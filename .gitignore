# Dependencies
node_modules/

# Logs
logs/
*.log

# Sensitive files - NEVER commit these
identity.json
identity.ruozhuo.json
identity.ruolan.json
identity.hermes.example.json
# 🔒 v5.0.1: 握手私钥/密钥材料（参考 csb-security/.gitignore）
data/mingde-private-key.pem
data/*-private-key.pem
data/*-key.pem
data/demo/private-key.pem
**/*-private-key.pem
.env
.env
.env.*
*.pem
*.key

# Heritage registry data - each agent has its own
heritage-registry.json

# Temp files
*.tmp
*.pid
.DS_Store
*.jpeg

# 备份文件/目录（本地防误还原，不入库）
*.bak
*.bak.*
backups/

# 运行时数据 - 不入库
logs/
data/a2a-tasks.json
monitor-state.json
aip-warmth.json

# Audit runtime logs
data/audit/

# Security keys (NEVER commit)
config/security/*.pem
config/security/*.key
-e 
# 运行时数据（勿提交）
data/a2a-tasks.json
topics-used.json

# 🔒 data/ 运行时数据目录（2026-09-09 一澜拍板）：各实例自持，不跟踪不进 git
# 背景：data/a2a-tasks.json 曾因被跟踪 + 仓库删除导致实例 pull 后数据文件被移除（tasks 1614→0）
# 规则：data/ 下一切运行时数据（task 存储/注册表快照/用户公钥等）永不入库
data/

# a2a-v5.env — 运行时 env 单一真相源（含 handshake 私钥路径 / 端口 / 对端公钥）
# 9/14 加：跟 .env / .env.a2a 性质类似（不能入库）
a2a-v5.env

# 9/14 若兰红线 P-1 · 卫生债清理
# 本机快照 / 内网拓扑 / 老版本备份——不入仓
*.bak
*.bak.*
known-agents.json

# config/security/*-aid.json — 各 agent 的 AID 文档（含内网 endpoint / 内网 IP）
# 9/14 若兰红线 P-1 · 卫生债清理
# 仓里留 <slug>-aid.example.json 模板；实例启动时由 setup-handshake.sh 生成实例专属 aid.json
# 注意：.pem 私钥已在更上方的 config/security/*.pem 屏蔽
config/security/*-aid.json

# 2026-09-14 加固（试点小虾发现）：identity 全部变体一律不入仓
identity.json*
!identity.json.example
